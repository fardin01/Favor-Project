<% @favor_activities.each do |activity| %>
  <%= div_for activity do %>
    <%= link_to activity.user.firstname, activity.user %>
    <%= render "activities/#{activity.trackable_type.underscore}/#{activity.action}", activity: activity %>
  <% end %>
<% end %>
